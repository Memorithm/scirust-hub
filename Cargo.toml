[workspace]
resolver = "2"
members = [
    "crates/hub-core",
    "crates/hub-protocol",
    "crates/hub-executor",
    "crates/hub-store-sqlite",
    "crates/hub-mcp",
    "crates/hub-api",
    "apps/scirust-hub",
    "apps/scirust-hubd",
    "apps/scirust-hub-worker",
]

[workspace.package]
version = "0.2.0"
edition = "2021"
rust-version = "1.89"
publish = false
repository = "https://github.com/Memorithm/scirust-hub"

[workspace.dependencies]
hub-core = { path = "crates/hub-core" }
hub-protocol = { path = "crates/hub-protocol" }
hub-executor = { path = "crates/hub-executor" }
hub-api = { path = "crates/hub-api" }
hub-store-sqlite = { path = "crates/hub-store-sqlite" }
hub-mcp = { path = "crates/hub-mcp" }

serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
uuid = { version = "1", features = ["v4", "serde"] }
sha2 = "0.10"
clap = { version = "4", features = ["derive", "env"] }
axum = { version = "0.8", default-features = false, features = ["http1", "json", "tokio", "query"] }
axum-server = { version = "0.8", features = ["tls-rustls"] }
rustls = { version = "0.23", default-features = false, features = ["aws_lc_rs"] }
tokio = { version = "1", features = ["rt-multi-thread", "net", "macros", "signal", "time"] }
tower = { version = "0.5", features = ["util"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
ureq = { version = "2", default-features = false, features = ["json", "tls", "native-certs"] }

[workspace.lints.rust]
unsafe_code = "forbid"

[profile.release]
lto = "thin"
