[package]
name = "hub-executor"
description = "Executor backends for SciRust Hub: capped local process execution and deterministic mock"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
repository.workspace = true

[dependencies]
hub-core = { workspace = true }

[lints]
workspace = true
